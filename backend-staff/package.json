{
  "name": "backend-staff",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "json-server staffdb.json --port 4000",
    "start": "json-server --host 0.0.0.0 --watch staffdb.json --port $PORT"
  },
  "type": "commonjs",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}